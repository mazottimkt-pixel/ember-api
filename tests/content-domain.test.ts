import {describe,expect,it} from "vitest";import {brandProfileSchema,briefingHash,contentBriefingSchema,contentStatuses,generatedContentSchema,imageBriefingSchema,imageSize,moderateContentInput} from "@/lib/content/domain";
const id="11111111-1111-4111-8111-111111111111";describe("content marketing domain",()=>{it("validates lightweight brand onboarding",()=>expect(brandProfileSchema.parse({segment:"Tecnologia",audience:"Pequenas empresas"}).voiceTone).toBe("professional"));it.each(["instagram_post","caption","reels","stories","campaign","ideas","calendar"] as const)("validates %s briefing",type=>expect(contentBriefingSchema.parse({type,objective:"Divulgar serviço",subject:"Consultoria",audience:"Empresas",tone:"Profissional",requestId:id}).type).toBe(type));it("does not accept absent objective or audience",()=>expect(()=>contentBriefingSchema.parse({type:"caption",requestId:id})).toThrow());it("limits hashtags",()=>expect(()=>generatedContentSchema.parse({title:"A",body:"B",cta:"C",hashtags:Array(13).fill("#x")})).toThrow());it("preserves informed offer fields without inventing defaults",()=>{const b=contentBriefingSchema.parse({type:"campaign",objective:"Promoção",subject:"Serviço",audience:"Clientes",tone:"Direto",requestId:id});expect(b.price).toBeUndefined();expect(b.offer).toBeUndefined();});it.each([["square","1024x1024"],["vertical","1024x1536"],["story","1024x1536"],["horizontal","1536x1024"]] as const)("maps %s safely",(format,size)=>expect(imageSize(format)).toBe(size));it("blocks unsafe image intent",()=>expect(moderateContentInput("quero falsificar documento").allowed).toBe(false));it("accepts appropriate business image",()=>expect(moderateContentInput("imagem de uma cafeteria moderna").allowed).toBe(true));it("creates stable briefing hash",()=>expect(briefingHash({a:1})).toBe(briefingHash({a:1})));it("has no published state",()=>expect(contentStatuses).not.toContain("published" as never));it("validates concise image text",()=>expect(imageBriefingSchema.parse({objective:"Divulgar",subject:"Café",format:"square",style:"modern",audience:"Adultos",requestId:id}).format).toBe("square"));});
