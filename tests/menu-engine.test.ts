import {describe,expect,it} from "vitest";import {MENU_VERSION,destination,menus,navigationState,renderMenu,resolveMenuInput} from "@/lib/navigation/menu-engine";
describe("versioned menu engine",()=>{it("renders six macro areas",()=>{const text=renderMenu("main");for(const label of ["Comercial","Operacional","Financeiro e documentos","Conteúdo e marketing","Consultas e gestão","Falar com a Lume"])expect(text).toContain(label);});it.each([["1","show_commercial"],["2","show_operations"],["4","show_content"],["6","talk_to_lume"]])("resolves main option %s",(value,action)=>expect(resolveMenuInput(value,navigationState("main"))?.action).toBe(action));it("resolves numbers only in current context",()=>{expect(resolveMenuInput("2",navigationState("commercial"))?.action).toBe("create_purchase_order");expect(resolveMenuInput("2",navigationState("main"))?.action).toBe("show_operations");});it("rejects delayed action from another submenu",()=>expect(resolveMenuInput("Criar pedido de compra",navigationState("management_queries"))).toBeNull());it.each(["Voltar","Menu principal","Início","Cancelar"])("recognizes global navigation %s",value=>expect(resolveMenuInput(value,navigationState("commercial"))).not.toBeNull());it("keeps commercial flows active and complete name",()=>{expect(renderMenu("commercial")).toContain("Criar pedido de compra");expect(menus.commercial.items.find(i=>i.id==="create_quote")?.available).toBe(true);});it("does not expose incomplete WhatsApp content actions",()=>expect(menus.content_marketing.items.filter(i=>i.action==="create_content"&&i.available)).toHaveLength(0));it("uses atomic version",()=>{expect(navigationState("main").menu_version).toBe(MENU_VERSION);expect(destination("show_content")).toBe("content_marketing");});});
