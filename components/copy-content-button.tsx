"use client";import{useState}from"react";export function CopyContentButton({text}:{text:string}){const[copied,setCopied]=useState(false);return<button className="button secondary" type="button" onClick={async()=>{await navigator.clipboard.writeText(text);setCopied(true)}}>{copied?"Copiado":"Copiar legenda"}</button>}
