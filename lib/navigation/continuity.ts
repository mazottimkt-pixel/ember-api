import type { MenuAction, MenuId } from "./menu-engine";
export type ContinuationKind="query_success"|"empty"|"recoverable_error"|"completed"|"cancelled"|"unavailable"|"free_reply";
export type ContinuationOption={action:MenuAction;label:string};
export function continuationOptions(kind:ContinuationKind,currentAction?:MenuAction):ContinuationOption[]{const primary=currentAction?{action:currentAction,label:kind==="recoverable_error"?"Tentar novamente":"Consultar novamente"}:undefined,common:ContinuationOption[]=[{action:"back",label:"Voltar"},{action:"show_main",label:"Menu principal"},{action:"talk_to_lume",label:"Falar com a Lume"}];if(kind==="cancelled"||kind==="unavailable")return common;return primary?[primary,...common]:common;}
export function renderNavigableResponse(body:string,input:{kind:ContinuationKind;currentMenu:MenuId;currentAction?:MenuAction;options?:ContinuationOption[]}){const options=input.options??continuationOptions(input.kind,input.currentAction),prompt=input.kind==="empty"?"Você pode:":input.kind==="recoverable_error"?"Você pode tentar novamente ou escolher outro caminho.":"O que deseja fazer agora?";return{reply:`${body.trim()}\n\n${prompt}\n\n${options.map((o,i)=>`${i+1} — ${o.label}`).join("\n")}`,options};}
