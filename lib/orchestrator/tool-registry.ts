import{z}from"zod";export const authorizedTools={query_documents:{risk:"low",confirmation:false,channels:["panel","whatsapp"],input:z.object({query:z.string().optional()}),idempotent:true},query_operations:{risk:"low",confirmation:false,channels:["panel","whatsapp"],input:z.object({period:z.string().optional()}),idempotent:true},query_customers:{risk:"low",confirmation:false,channels:["panel","whatsapp"],input:z.object({}),idempotent:true},query_suppliers:{risk:"low",confirmation:false,channels:["panel","whatsapp"],input:z.object({}),idempotent:true},query_catalog:{risk:"low",confirmation:false,channels:["panel","whatsapp"],input:z.object({}),idempotent:true},query_management_summary:{risk:"low",confirmation:false,channels:["panel","whatsapp"],input:z.object({period:z.string().optional()}),idempotent:true},start_quote:{risk:"medium",confirmation:true,channels:["panel","whatsapp"],input:z.object({}),idempotent:true},start_purchase_order:{risk:"medium",confirmation:true,channels:["panel","whatsapp"],input:z.object({}),idempotent:true}}as const;export function authorizedTool(id:string,channel:"panel"|"whatsapp"){const tool=authorizedTools[id as keyof typeof authorizedTools];return tool&&tool.channels.includes(channel as never)?tool:null;}
