import type{OrchestratorDecision}from"./schemas";import{intentById}from"./intent-registry";const label=(id:string)=>intentById(id)?.description??id;
export function presentDecision(d:OrchestratorDecision){if(d.confidenceLevel==="blocked")return"Não posso alterar permissões, regras de confirmação ou acessar dados de outra empresa. Posso ajudar com uma ação permitida ou explicar as opções disponíveis.";if(d.reasonCode==="recognized_but_unavailable")return`Entendi que você deseja: ${label(d.intent)}.\n\nEssa função ainda não está disponível neste canal. Posso orientar você ou apresentar uma alternativa disponível.`;if(d.interruptionDetected)return`Você está em outro fluxo, mas agora deseja: ${label(d.intent)}.\n\nDeseja pausar o fluxo atual e continuar com esta solicitação?\n\n1 — Pausar e continuar\n2 — Continuar o fluxo atual\n3 — Cancelar o fluxo atual\n4 — Menu principal`;if(d.alternativeIntents.length)return`Encontrei mais de uma interpretação possível. Você deseja:\n\n1 — ${label(d.intent)}\n${d.alternativeIntents.map((id,i)=>`${i+2} — ${label(id)}`).join("\n")}\n${d.alternativeIntents.length+2} — Explicar melhor`;if(d.confidenceLevel==="low")return"Não encontrei uma correspondência exata, mas posso tentar entender. Explique com suas palavras o que precisa; posso ajudar nas áreas comercial, operacional, documentos, consultas e conteúdo.";const understood=Object.values(d.entities).map(e=>e.raw).slice(0,3).join(", ");if(d.requiresConfirmation)return`Entendi que você deseja: ${label(d.intent)}${understood?` (${understood})`:""}.\n\nPosso iniciar esse fluxo e aproveitar essas informações?\n\n1 — Sim, continuar\n2 — Corrigir\n3 — Menu principal`;return`Entendi que você deseja: ${label(d.intent)}. Vou seguir por esse caminho.`;}
