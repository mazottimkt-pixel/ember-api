import {createClient} from "@supabase/supabase-js";import {loadLocalEnv} from "./load-env.mjs";
const env=loadLocalEnv();for(const key of ["NEXT_PUBLIC_SUPABASE_URL","SUPABASE_SERVICE_ROLE_KEY","TEST_OWNER_EMAIL","TEST_OWNER_PASSWORD"])if(!env[key])throw new Error(`Variável ausente: ${key}`);
const supabase=createClient(env.NEXT_PUBLIC_SUPABASE_URL,env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});
let owner=null;for(let page=1;page<=10&&!owner;page++){const{data,error}=await supabase.auth.admin.listUsers({page,perPage:100});if(error)throw new Error(`Auth indisponível: ${error.message}`);owner=data.users.find(user=>user.email?.toLowerCase()===env.TEST_OWNER_EMAIL.toLowerCase());if(data.users.length<100)break}
let created=false;if(!owner){const{data,error}=await supabase.auth.admin.createUser({email:env.TEST_OWNER_EMAIL,password:env.TEST_OWNER_PASSWORD,email_confirm:true,user_metadata:{full_name:"Proprietário Ember"}});if(error)throw new Error(`Falha ao criar usuário: ${error.message}`);owner=data.user;created=true}
const userClient=createClient(env.NEXT_PUBLIC_SUPABASE_URL,env.NEXT_PUBLIC_SUPABASE_ANON_KEY,{auth:{persistSession:false,autoRefreshToken:false}});const{error:loginError}=await userClient.auth.signInWithPassword({email:env.TEST_OWNER_EMAIL,password:env.TEST_OWNER_PASSWORD});if(loginError)throw new Error("Falha ao autenticar proprietário de teste");let{data:memberships,error:membershipError}=await userClient.from("organization_members").select("organization_id,role");if(membershipError)throw new Error(`Falha ao consultar organização: ${membershipError.message}`);let organizationCreated=false;if(!memberships?.length){const{error}=await userClient.rpc("create_organization",{org_name:env.TEST_ORGANIZATION_NAME});if(error)throw new Error(`Falha no onboarding: ${error.message}`);organizationCreated=true;({data:memberships,error:membershipError}=await userClient.from("organization_members").select("organization_id,role"));if(membershipError)throw new Error("Falha ao validar onboarding")}
console.log(JSON.stringify({authConnected:true,ownerReady:Boolean(owner),userCreated:created,organizationReady:memberships?.length===1,organizationCreated,ownerRole:memberships?.[0]?.role==="owner"}));
