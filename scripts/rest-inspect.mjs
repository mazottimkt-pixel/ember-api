import {loadLocalEnv} from "./load-env.mjs";const env=loadLocalEnv();const response=await fetch(`${env.NEXT_PUBLIC_SUPABASE_URL}/rest/v1/`,{headers:{apikey:env.SUPABASE_SERVICE_ROLE_KEY,authorization:`Bearer ${env.SUPABASE_SERVICE_ROLE_KEY}`,accept:"application/openapi+json"}});if(!response.ok)throw new Error(`REST schema indisponível: HTTP ${response.status}`);const schema=await response.json();const tables=Object.keys(schema.definitions??{}).sort();console.log(JSON.stringify({restConnected:true,tableCount:tables.length,tables}));
