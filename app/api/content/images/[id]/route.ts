import{NextResponse}from"next/server";import{requireMembership}from"@/lib/auth/session";import{signedContentImage}from"@/lib/content/service";import{z}from"zod";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;z.uuid().parse(id);const{supabase,organizationId,user}=await requireMembership();return NextResponse.redirect(await signedContentImage({supabase,organizationId,userId:user.id},id));}
