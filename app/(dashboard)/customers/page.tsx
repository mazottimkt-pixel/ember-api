import {requireMembership} from "@/lib/auth/session";import {saveParty,deleteParty} from "../crud-actions";
export default async function Page(){const{supabase}=await requireMembership();const{data=[]}=await supabase.from("customers").select("id,name,tax_id,email,phone").is("deleted_at",null).order("name");return <><h1>Clientes</h1><form className="panel form-grid" action={saveParty.bind(null,"customers")}><div className="field"><label>Nome</label><input name="name" required/></div><div className="field"><label>CPF/CNPJ</label><input name="tax_id"/></div><div className="field"><label>E-mail</label><input name="email" type="email"/></div><div className="field"><label>Telefone</label><input name="phone"/></div><button className="button">Adicionar cliente</button></form><div className="panel" style={{marginTop:16}}><table className="table"><tbody>{data?.map(item=><tr key={item.id}><td>{item.name}</td><td>{item.email||"—"}</td><td><form action={deleteParty.bind(null,"customers")}><input type="hidden" name="id" value={item.id}/><button className="link-button">Excluir</button></form></td></tr>)}</tbody></table></div></>}
