import Link from "next/link";import {requireMembership} from "@/lib/auth/session";import {formatBRL} from "@/lib/domain/calculations";
export default async function Documents(){const{supabase}=await requireMembership();const{data=[]}=await supabase.from("documents").select("id,number,type,status,total,counterparty_snapshot,created_at").is("deleted_at",null).order("created_at",{ascending:false});return <><div className="topline"><h1>Documentos</h1><Link className="button" href="/documents/new">Novo documento</Link></div><div className="panel"><table className="table"><thead><tr><th>Número</th><th>Tipo</th><th>Destinatário</th><th>Total</th><th>Status</th></tr></thead><tbody>{data?.map(doc=><tr key={doc.id}><td><Link href={`/documents/${doc.id}`}>{doc.number}</Link></td><td>{doc.type==="quote"?"Orçamento":"Pedido"}</td><td>{String((doc.counterparty_snapshot as {name?:string})?.name??"—")}</td><td>{formatBRL(Number(doc.total))}</td><td><span className="status">{doc.status}</span></td></tr>)}</tbody></table></div></>}
